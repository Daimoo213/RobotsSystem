# CODEBUDDY.md This file provides guidance to CodeBuddy when working with code in this repository.

## 项目概述

本项目是**机器人集群智能调度系统**——某高校"智能建造"科研展示项目的"大脑"部分。系统调度 20–100 台异构施工机器人（AGV 运输车、挖掘机、吊车、砌筑机器人、巡检机器人等），在微缩工地沙盘上协同完成房地产建设全流程演示。

**我们的交付范围：** 调度系统的前后端，以及提供给机器人硬件接入的标准化 API 接口。不涉及机器人硬件本身的实现——我们只提供机器人连接调度的接口。

**两套前端界面：** 系统分为 PM 端（项目经理界面，面向管理与指挥）与 O&M 端（运维人员界面，面向设备运维与现场调度）两个独立 Web 大屏，二者共用同一后端与同一套机器人接入 API。参考样式位于 `前端样式参考/` 目录（`PM端施工进度/index.html` 与 `设备集群3D土方调度/equipment-3d-dashboard.html`）。

| 界面 | 面向对象 | 核心定位 | 参考样式 |
|------|---------|---------|---------|
| **PM 端** | 项目经理 / 参观讲解 | 施工进度指挥、任务分配、整体态势 | `前端样式参考/PM端施工进度/` |
| **O&M 端** | 运维人员 / 现场调度 | 设备集群运维、3D 调度监控、应急处置 | `前端样式参考/设备集群3D土方调度/` |

## 常用命令

### 项目初始化
```bash
# 安装后端依赖
cd backend && pip install -r requirements.txt

# 安装前端依赖
cd frontend && npm install
```

### 开发调试
```bash
# 启动后端开发服务器（热重载）
cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 启动前端开发服务器
cd frontend && npm run dev

# 运行全部后端测试
cd backend && pytest

# 运行单个测试文件
cd backend && pytest tests/test_scheduler.py -v

# 前端代码检查
cd frontend && npm run lint
```

### 数据库迁移
```bash
# 模型变更后生成迁移文件
cd backend && alembic revision --autogenerate -m "变更描述"

# 应用迁移
cd backend && alembic upgrade head
```

### 生产部署
```bash
docker-compose up -d
```

## 架构设计

### 四层总体架构

系统采用严格的四层架构，数据自底向上（设备状态→感知→决策→执行）和自顶向下（指令→执行→反馈）双向流动，形成"感知—决策—执行—反馈"闭环：

```
┌──────────────────────────────────────────────────────┐
│  应用层                                              │
│  - 数字孪生大屏（三维沙盘镜像）                        │
│  - 监控与告警中心                                     │
│  - 施工数据报表统计                                   │
│  - 人机交互（任务干预、优先级覆盖）                     │
├──────────────────────────────────────────────────────┤
│  调度层（核心大脑）                                    │
│  - 任务编排引擎（基于 DAG）                            │
│  - 调度策略（集中式 + 分布式混合）                     │
│  - 协同作业控制（编队、碰撞联锁）                       │
│  - 能耗与维保管控                                     │
│  - 安全管控（急停、电子围栏）                          │
│  - 工地地图与环境感知                                  │
├──────────────────────────────────────────────────────┤
│  接入层                                              │
│  - 设备注册与集群管理                                  │
│  - 协议适配（ROS2、MQTT）                             │
│  - 心跳与遥测数据采集                                  │
├──────────────────────────────────────────────────────┤
│  设备层（物理层——非我方代码）                          │
│  - AGV、挖掘机、吊车、砌筑机器人、巡检机器人等           │
└──────────────────────────────────────────────────────┘
```

### 八大核心功能模块

以下 8 个模块并非彼此孤立，而是通过上述闭环数据流紧密协作。

#### 1. 设备接入与集群管理
- **定位：** 全部机器人数据的入口。
- **核心职责：** 支持 20–100 台施工设备并发接入；设备注册采用"一机一档"模式，记录型号、能力、适配场景、通行/施工权限；按"标段/车型/工序"三维标签进行分组管理；通过心跳 + 遥测（位置、电量、任务状态）实现实时状态监测。
- **实现方式：** 以 ROS2 节点 + 适配器模式对接不同厂商设备，统一抽象为 `DeviceAgent`（设备代理）。设备上线通过 MQTT/ROS2 主题广播，调度中心自动发现并写入设备注册表（内存 + 持久化）。
- **数据流向：** 为地图感知提供设备实时坐标 → 为智能调度提供可调度资源清单 → 为能耗维保提供电量/里程 → 为安全管控提供设备在线状态。

#### 2. 工地地图与环境感知
- **定位：** 所有路径规划和任务分配的空间基础。
- **核心职责：** 二维网格 + 高度层沙盘地图，支持按施工进度做版本化快照；动态障碍物标记（临时堆放、人员）；施工点位标注（基坑、材料区、吊装位）并绑定工序类型与适配设备；区域划分：作业区/禁行区/堆放区/停靠区。
- **实现方式：** 地图编辑器支持区域和点位管理；融合二维码/SLAM 定位与传感器数据实现动态障碍物实时更新。
- **数据流向：** 输出的"地图 + 点位 + 障碍"是智能调度做路径规划与派单的前提 → 坐标更新同步给数字孪生大屏渲染。

#### 3. 集群智能调度（核心模块）
- **定位：** 系统的中枢大脑，消费施工进度计划，产出任务分配与路径。
- **核心职责：**
  - **任务模型：** 将全部 29 项施工工序（从"场地平整"到"质量检测"）抽象为 **DAG（有向无环图）**，每个节点包含前置约束、所需设备类型、预估工期。
  - **调度策略：** 集中式求解全局派单与工序约束，分布式协商解决局部资源争用与避碰。
  - **任务分配：** 市场机制（设备对任务"投标"，调度器按成本 + 优先级定标），预留策略接口可切换为强化学习分配。
  - **任务模板：** 将"某标段土方作业"等常见工作流固化为模板，支持一键派单与批量下发。
  - **优先级体系：** 默认优先级：土方作业 > 压实作业 > 构件吊装 > 物料转运 > 砌筑/抹灰/地坪 > 场地清扫 > 现场巡检。运行时可人工临时调整。
  - **人工干预：** 支持暂停/改派/插单，干预记录全程留痕。
- **调度约束因素：** 设备类型是否适配工序、当前位置与任务点距离、剩余电量/续航、所属标段权限、人工优先级覆盖。
- **数据流向：** 消费地图/点位做可行性判断 → 产出派单与路径给协同控制 → 受能耗约束（低电量不派长途）和安全约束（禁行区不派）→ 结果进入监控告警和报表统计。

#### 4. 集群协同作业控制
- **定位：** 将调度决策转译为设备可执行指令。
- **核心职责：** 批量操控（对同标段/同型设备下发群组指令）、编队同步（对齐多机节拍，如吊装+转运协同）、碰撞联锁（设备间实时交换位姿，进入安全距离触发减速/停车）。
- **实现方式：** 群组指令下发，联锁事件上报监控告警与安全管控。

#### 5. 能耗与维保管控
- **定位：** 保障集群可持续稳定演示。
- **核心职责：** 能耗模型（按设备类型/负载估算耗电）、剩余续航预测、错峰充能调度（在设备空闲或低优先级任务间隙调度充电，避免集中断电影响演示）、维保提醒（按运行时长/里程触发保养工单）。
- **数据流向：** 为智能调度提供"可调度性"约束（低电量设备降权）→ 数据进入报表统计故障率。

#### 6. 监控与告警中心
- **定位：** 所有系统状态的聚合层，驱动数字孪生大屏。
- **核心职责：** 数字孪生大屏实时镜像物理沙盘（设备位姿、路径、任务、告警）；单设备下钻查看（轨迹、电量、当前任务、历史告警）；告警引擎订阅各模块事件（离线、超时、低电量、路径冲突、越界），分级推送并关联处置闭环。
- **实现方式：** 发布/订阅模式（ROS2 主题 / WebSocket）将调度层状态推送到前端，做到"物理沙盘发生什么，大屏同步什么"。

#### 7. 施工数据与报表统计
- **定位：** 演示后的数据分析与汇报支撑。
- **核心职责：** 事件溯源——调度与执行全过程写入时序库作为统计底座；多维度聚合（按标段/日期/工序/车型/班组）；一键导出标准化报表（PDF/Excel）。
- **数据流向：** 消费所有模块的运行数据，支撑项目复盘与调度策略优化。

#### 8. 施工现场安全管控
- **定位：** 全系统的安全底线。
- **核心职责：** 人机避让（基于实时位姿与电子围栏，进入危险区触发降速或停机）；全局急停（一键对所有在线设备下发急停）；分区管控（按标段划分电子围栏，跨区作业需授权，避免并行施工干扰）。
- **数据流向：** 对智能调度和协同控制施加安全约束（禁行、限权）→ 急停/越界事件上报监控告警。

## 两套前端界面功能详解

以下功能均来自 `前端样式参考/` 中的参考样式，是前端必须实现的内容。后端需在对应模块提供数据支撑。

### PM 端（项目经理界面 —— 施工进度指挥端）

参考文件：`前端样式参考/PM端施工进度/index.html`

整体为"三栏大屏"：左栏（阶段健康度 + 资源负载 + 阶段总览）、中栏（任务调度甘特图）、右栏（待办任务 + 摄像头视角 + 完成趋势 + 实时事件）。顶部状态栏展示系统时间、设备在线率、扬尘、工期健康分、当前用户。

**1. 顶部状态栏**
- 标题品牌区 + 当前项目名（如"龙湖世纪城项目 · 施工进度指挥端"）
- 实时状态：日期时间、设备在线率（`在线 24/24`）、扬尘等级、工期健康分、当前登录 PM 用户名

**2. 核心 KPI 指标条（8 项）**
- 总体进度、土方工程完成率、进行中任务数、待闭环事项数、延期风险数、资源负载率、渣土外运车次、AI 识别合规率

**3. 左栏面板**
- **各类机器人使用数量**：环形图/柱状图展示各类型机器人当前投入使用的数量分布（AGV 运输车、挖掘机、吊车、砌筑机器人、巡检机器人等），含图例与数量/占比
- **土方资源负载**：设备/班组负载条形图（挖机、渣土车队、测量班组、喷淋降尘、夜间照明、安全巡检）
- **施工阶段总览**：7 大阶段（土方开发→室外景观）进度条，未到达阶段仅显示大进度

**4. 中栏：任务调度甘特图（核心）**
- 支持日/周/月/季度时间粒度切换
- 过滤标签：土方优先、仅风险、导出、图层
- 甘特图含阶段行 + 任务行（带任务编码、名称、进度百分比、起止位置）
- 今日线标记（`今天 06-12`）+ 关键里程碑菱形标记
- 颜色图例：已完成 / 收尾施工 / 后续阶段 / 关键里程碑 / 延期风险

**5. 右栏面板**
- **待办任务**：任务清单，支持 `+ 新增`；每条带紧急/高/AI/闭环/巡检等标签（紧急、今日、AI、财务、审批、巡检、影像、环保、安全、计划、合约、实名制等分类标签）
- **摄像头视角**：实时 CCTV 画面（如 CAM-03 东南角基坑），叠加 AI 识别浮窗（挖机数、渣土车数、人员数、扬尘指数、边坡风险）、REC 标识、作业面说明、网络延迟
- **任务完成趋势**：计划/实际/预测三线折线图（SVG）
- **实时事件**：闭环事件流（时间 + 事件描述 + 状态标签）

**6. PM 端特有交互**
- 待办任务新增/编辑
- 甘特图视图切换与筛选
- 摄像头 AI 识别结果展示

---

### O&M 端（运维人员界面 —— 设备集群 3D 调度端）

参考文件：`前端样式参考/设备集群3D土方调度/equipment-3d-dashboard.html`

整体为"三栏 + 底部设备条"布局：左栏（设备状态分布 + 运维操作台 + 实时运行日志）、中栏（3D 土方开挖调度视角）、右栏（集群协同拓扑 + 设备类型分布 + 实时告警 + 系统概览）、底部（18 张设备状态卡片条）。顶部含设备在线/故障/健康度统计，以及"调度视图/能耗视图/安全视图"标签页切换。

**1. 顶部状态栏**
- 标题"施工设备集群调度系统"
- 实时统计：在线设备数（`在线 20/20`）、故障数、健康度、运行时长计时
- 视图切换标签：调度视图 / 能耗视图 / 安全视图
- 当前用户标识（PM）

**2. 左栏面板**
- **设备状态分布**：环形图展示设备状态分类（待机、充电、移动、维护、作业、占用、故障）及数量
- **运维操作台**（快捷指令，8 个）：设备自检（全量诊断）、强制充电（路径最短）、模式切换（自动/手动）、系统诊断（异常定位）、数据备份（实时同步）、固件升级（OTA 分发）、网络检测（信号覆盖）、安全巡检（围栏校验）
- **实时运行日志**：最近事件流（时间 + 设备 + 事件描述 + 类型标签，如告警/占用/闭环/充电/同步/正常/更新/完成）

**3. 中栏：3D 土方开挖调度视角（核心）**
- 基于 Three.js 的透明空间方块视图：土方开挖坑底/放坡面/作业面、楼栋占位（1/2/3号楼）、临时道路、材料堆场、泥浆土站、设备停靠区、障碍围挡、安全缓冲区均为半透明模型
- 机器人以红点（告警态）或红标实时显示，叠加 HUD 网格、文字标签（楼栋名、坑底、主路、支路）、机器人名牌浮标（`T-03` 等）
- 场景工具栏命令按钮：**急停 / 暂停 / 释放 / 重置 / 恢复**（全局设备控制）
- 设备状态筛选芯片：作业 / 移动 / 待机 / 充电 / 故障 / 维修占用
- 场景说明浮窗：透明空间方块视图含义说明

**4. 右栏面板**
- **集群协同拓扑**：20 节点拓扑图（中心主节点 + 多个子节点 + 连线），展示设备协作关系
- **设备类型分布**：按类型统计数量（运输 / 挖掘 / AGV / 巡检）
- **实时告警**：告警列表（设备 + 告警描述 + 等级色标：红/灰/黄/蓝/绿），统一告警标准
- **系统概览（健康检测）**：节点在线率、任务完成率、平均延迟、云端连接状态

**5. 底部设备状态条（18 张卡片）**
- 每张卡片：设备类型 + 状态标签（故障报警/待机/告警复核/移动/作业/充电/巡检/维修占用）、设备 ID、统一接入标准说明（`连接≤2s · 定位≤15cm · 电量≥20% · 任务/安全链路正常`）、5 项体检指标（连/定/电/任/安，状态：ok/warn/fail/busy）

**6. O&M 端特有交互**
- 全局设备命令（急停/暂停/释放/重置/恢复）
- 运维快捷指令触发
- 设备状态筛选（按作业/移动/待机/充电/故障/维修占用）
- 单设备下钻（点击卡片查看详情、体检指标）
- 调度/能耗/安全三视图切换

### 双端共用能力（后端统一支撑）
- 设备实时状态聚合（来自设备接入模块）
- 告警引擎事件订阅（来自监控告警模块）
- 地图与三维坐标数据（来自地图感知模块）
- 任务/进度数据（来自智能调度模块）
- 摄像头 AI 识别结果（来自地图感知 + 安全模块融合）

### 模块间数据流关系（关键理解）

八个模块形成如下协作链路：

```
设备接入 ──汇聚全部实时状态──→ 地图感知、能耗维保、安全管控
地图感知 ──提供地图/点位/障碍──→ 智能调度
智能调度 ──产出派单+路径，受约束于──→ 能耗维保、安全管控
智能调度 ──输出任务+路径──→ 协同控制
协同控制 ──碰撞事件──→ 安全管控、监控告警
监控告警 ──聚合所有状态+事件──→ 数字孪生大屏
数据报表 ──沉淀全流程数据──→ 反哺调度策略优化
```

**调度主流程一句话：** 施工进度计划 → 拆解任务（DAG）→ 地图校验 → 约束求解（能耗/安全）→ 生成派单+路径 → 协同执行 → 实时反馈 → 异常重规划 → 大屏呈现 + 报表沉淀。

### 关键技术决策

- **通信框架：** 以 ROS2 为主，MQTT 用于设备遥测，WebSocket 用于前端实时推送。
- **调度架构：** 集中式（全局最优派单）+ 分布式（局部避碰协商）混合模式。
- **任务分配：** 市场机制（招标/竞价），预留策略接口可切换为强化学习。
- **定位方式：** 二维码 + SLAM 融合定位，提供设备在沙盘地图上的坐标。
- **前端形态：** 数字孪生三维可视化大屏，非传统 Web 应用——实时镜像物理沙盘。O&M 端 3D 场景使用 Three.js（参考样式中已引入 `assets/three.module.js`）渲染透明空间方块模型与机器人实时位置；PM 端以 SVG/Canvas 绘制甘特图、环形图、折线图。
- **并发目标：** 同时支持 20–100 台设备接入与调度。
- **前端技术栈建议：** 参考样式为纯 HTML/CSS/JS（无框架），正式工程建议采用 React/Vue + Vite 构建双端大屏，3D 场景沿用 Three.js；实时数据通过 WebSocket 订阅后端推送。

### 29 项施工工序体系

29 项工序按执行阶段组织：

| 阶段 | 包含工序 |
|------|---------|
| 开工准备 | 场地平整 |
| 基础施工 | 基坑土方清运、渣土外运、桩基施工 |
| 主体施工 | 钢筋绑扎、模板安装、混凝土浇筑、混凝土养护、预制构件吊装、钢筋加工配送、楼层砌筑、楼层垂直运输 |
| 机电安装 | 管线安装、桥架安装、电缆敷设 |
| 装修施工 | 内墙抹灰、墙面喷涂、地坪打磨找平、墙面打磨、瓷砖铺贴、吊顶安装 |
| 全周期 | 建材辅料转运、施工垃圾清运、场地文明规整、现场安全巡检、临边看护巡查、应急抢险补给、测量放线、质量检测 |

任务以 DAG 形式组织，包含前置约束关系。调度引擎在实例化和分配任务时必须遵守此 DAG 的依赖关系。

### 机器人 API 设计原则

我们提供标准化的 RESTful/ROS2 API 供机器人使用，包括：
- 设备注册与心跳上报
- 遥测数据上报（位置、电量、状态）
- 接收任务指派（任务 ID、目标位置、动作参数）
- 上报任务进度与完成状态
- 响应紧急指令（暂停、恢复、急停）

API 层采用适配器模式——将各厂商机器人的私有协议归一化到统一接口之后，调度引擎完全不接触厂商特定细节。

### 演示剧本管理

系统需支持切换不同的"演示剧本"——预定义的施工进度计划，展示智能建造的不同侧面。剧本定义了执行 29 项工序中的哪些子集、以什么顺序、用哪些设备、以什么节奏推进。这是任务编排引擎中的一等概念。

### 项目背景须知（非代码层面）

- 本项目面向高校科研展示——系统稳定性和视觉效果优先于极端场景性能。
- 沙盘是微缩物理工地，数字孪生大屏是面向参观者的主要展示输出。
- 教学部分和实验部分（机械臂砌砖）与本系统分属不同板块，但共享"智能建造"主题。
- 系统需在演示后生成标准化报表（PDF/Excel），供学术汇报与复盘使用。
