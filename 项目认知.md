# 广科大项目 · 项目认知

> 本文档用于统一对项目背景、技术构成与目标的理解，作为后续"开发方案确认"与"方案制作"的基线。
> 最后更新：2026-07-14（补充第六~九章：本系统开发决策、ADR、Non-Goals、待确认要点更新）

## 一、项目定位

- **类型**：学校科研展示大型项目（主题：智能建造）
- **目标**：为学校定制一套"智能建造"主题的科研/教学展示方案，范围涵盖方案设计、软件开发、硬件采购与系统集成。
- **两大板块**：科研部分、教学部分（教学部分又细分为"教学子模块"与"实验子模块"）。

## 二、科研部分：沙盘模拟 + 机器人集群智能调度

**展示形式**：规划专门场地，搭建微缩房地产建设工地沙盘，多台智能机器人在沙盘上协同作业，配合"机器人集群智能调度系统"完成房地产建设全流程演示。

**核心构成**

1. **沙盘模型**：微缩工地（楼宇、道路、材料区、施工区、塔吊等），作为物理展示载体与机器人运行环境。
2. **智能机器人集群**：多台可移动/作业机器人（如运输 AGV、施工机器人、巡检机器人），在沙盘尺度上运行。
3. **集群智能调度系统（大脑）**：
   - 任务分配：将建设任务拆解并分配给各机器人；
   - 路径规划与避碰：多机协同路径、实时避障；
   - 协同决策：动态调度、异常重规划；
   - 可视化：数字孪生大屏，实时展示调度状态与工地态势。
4. **技术路线参考**：ROS2 / 多智能体系统（MAS）、集中式 + 分布式混合调度、强化学习或市场机制的任务分配、SLAM/二维码定位、数字孪生可视化。

**展示价值**：体现"智能建造"理念，直观呈现房地产建设全流程的自动化、协同化。

## 三、教学部分：机械臂砌砖（两个子模块）

### 3.1 教学子模块（编程教学）

- **硬件**：
  - 桌面级教学用机械臂（教育臂，如越疆 / 大族 / UFACTORY 等）；
  - 支架上俯视桌面的双目摄像头（提供深度 + 彩色，用于桌面视觉定位）。
- **任务**：学生通过编程，控制机械臂完成"搬砖"任务（识别 → 抓取 → 搬运 → 堆叠）。
- **技术要点**：
  - 双目视觉定位（颜色/形状识别、深度估计、3D 定位）；
  - 手眼标定（camera-robot calibration）；
  - 运动规划与抓取；
  - 视觉伺服（visual servoing）；
  - 教学编程（图形化 / Python）。
- **教具**：以积木块 / 教具砖代替真实砖，安全、低成本、易迭代。

### 3.2 实验子模块（真实场景模拟）

- **硬件**：
  - 工业级机械臂（如发那科 / 库卡 / ABB / 埃斯顿 / 节卡协作臂）；
  - 可移动底盘（AGV / AMR 或全向移动平台），使机械臂可在"工地"移动。
- **任务**：模拟真实施工现场，进行砌砖操作（取砖 → 砌筑 → 形成墙体）。
- **技术要点**：
  - 移动底盘导航与定位（SLAM / 二维码 / 激光）；
  - 臂 - 底盘协同；
  - 砌筑工艺规划（砖序、灰缝 / 砂浆模拟、贴合）；
  - 力控与安全（围栏、急停）。
- **与教学的区别**：尺度真实、负载大、含移动性、接近工程验证。

## 四、三部分的关系

| 维度 | 科研部分 | 教学子模块 | 实验子模块 |
|------|----------|------------|------------|
| 层级 | 系统层（多机调度展示） | 入门层（桌面视觉编程） | 应用层（工业臂 + 移动底盘） |
| 机器人/机械臂 | 沙盘多机器人集群 | 桌面教育臂 | 工业臂 + 移动底盘 |
| 视觉 | 调度/态势感知 | 双目俯视定位 | 导航/砌筑视觉 |
| 任务 | 房地产建设全流程 | 搬砖（学习） | 真实砌砖（验证） |
| 目的 | 展示/系统演示 | 教学/编程训练 | 实验/工程验证 |

**共同主题**：智能建造 —— 从宏观集群调度，到微观机械臂砌砖，再到真实场景验证，形成递进展示体系。

## 五、方案制定待确认要点（后续需与用户对齐）

1. **场地与规模**：沙盘尺寸、展示面积、参观动线。
2. **机器人数量与形态**：科研用几台？什么类型（AGV / 施工机器人 / 无人机）？
3. **调度系统技术路线**：自研 vs 商业软件；是否需要数字孪生大屏。
4. **教学机械臂选型/数量**；双目摄像头选型（如 Intel RealSense / 自研双目模组）；"砖"用真实砖还是教具块。
5. **实验机械臂与底盘**：品牌 / 负载；底盘类型（轨道 / AGV / 全向轮）；是否真实砌砖含砂浆还是模拟。
6. **砌砖工艺真实度**：砂浆、灰缝、墙体结构、精度要求。
7. **软件与接口**：各子系统是否统一中控平台；数据本地 / 上云。
8. **交付与验收**：软硬件清单、文档、培训、演示脚本。
9. **预算与采购范围**：哪些采购、哪些自研开发。
10. **时间节点与里程碑**。

---

## 六、集群调度系统开发决策（2026-07-14 已与用户确认）

> 本章记录"机器人集群智能调度系统"（即我方交付的软件部分）的关键开发决策。本系统属于科研部分的核心软件，独立于教学部分。

### 6.1 我方交付范围

- 调度系统的前后端（PM端 + O&M端 + 后端服务）。
- 提供给机器人硬件接入的标准化 API 接口。
- **不含**：机器人硬件本体实现、定位算法本身、真实CV识别算法。

### 6.2 关键技术决策清单

| 编号 | 决策项 | 确认结果 | 说明 |
|------|--------|---------|------|
| D1 | 设备接入 | Adapter 抽象层 + 内置仿真设备模拟器 | 解耦前端开发与真实机器人；真实设备接入只需新增 adapter |
| D2 | 协议分层 | REST（注册/指派/急停）+ WebSocket（遥测/状态推送，含前端）+ MQTT（仅真实设备遥测） | 演示阶段仿真器直接走 WebSocket，跳过 MQTT |
| D3 | 前端工程 | pnpm monorepo：`apps/pm` + `apps/om` + 共用 packages | 共用组件库、3D场景封装、API客户端 |
| D4 | 3D 集成 | @react-three/fiber + @react-three/drei | 声明式 Three.js，React 状态驱动 |
| D5 | 数据库 | PostgreSQL 16 + TimescaleDB（时序扩展）+ Redis（缓存） | 业务级数据库，兼顾实用性与演示；TimescaleDB 作为 PG 扩展无需单独引入 InfluxDB |
| D6 | 调度策略 | 一期实现市场机制（招标/竞价），预留 `SchedulerStrategy` 接口 | RL 强化学习作为二期扩展 |
| D7 | 演示剧本 | JSON/YAML 配置文件 + PM端顶部下拉切换 | 初期内置 2-3 个剧本（土方主导/主体施工/全流程精简） |
| D8 | 坐标系 | 2D网格(x,y) + 高度层(z 语义化) | 内部坐标用2D+高度层；3D场景单独配置模型占位 |
| D9 | 3D点云渲染 | **路径C：混合模式** | 后端同时支持"接收真实点云"和"内置点云生成器"，配置切换；演示阶段用生成器模拟SLAM建图过程，未来接真实设备切真实点云 |
| D10 | 摄像头AI识别 | 一期模拟数据 + 预留接口 | 后端按剧本节奏推送识别结果，未来接真实 CV 服务 |
| D11 | 报表导出 | 一期 Excel（events聚合），PDF 二期 | |
| D12 | 权限分级 | 方案2：PM端保留应急急停，恢复需O&M确认；其余机器人控制全在O&M端 | 见 6.3 权限矩阵 |
| D13 | 定位算法 | 只接收设备上报坐标 + 做越界校验，不做定位算法本身 | 定位由设备硬件完成 |

### 6.3 权限矩阵

| 操作 | PM端 | O&M端 |
|------|:----:|:-----:|
| 查看所有状态（设备/任务/告警/地图） | ✅ | ✅ |
| 应急急停（全局，仅应急用） | ✅ | ✅ |
| 急停后恢复/释放/重置 | ❌ | ✅ |
| 暂停/恢复（常规） | ❌ | ✅ |
| 运维快捷指令（自检/充电/模式切换/诊断/备份/升级/网络检测/巡检） | ❌ | ✅ |
| 设备状态筛选（作业/移动/待机/充电/故障/维修占用） | ❌ | ✅ |
| 任务管理（新增/编辑/改派/插单/暂停） | ✅ | ❌ |
| 剧本切换 | ✅ | ❌ |
| 甘特图视图切换与筛选 | ✅ | ❌ |
| 待办任务新增/编辑 | ✅ | ❌ |
| 报表导出 | ✅ | ✅ |

**权限规则**：
- PM端可下"应急急停"，但触发后系统进入急停态，**必须由 O&M 端执行恢复**。
- 所有常规机器人控制（暂停/释放/重置/恢复/运维指令/状态筛选）均在 O&M 端。
- 任务编排与剧本管理在 PM 端。
- 两端都能查看全部状态和导出报表。

### 6.4 3D点云渲染（路径C）实现要点

- **后端**：
  - 提供 `PointCloudSource` 抽象接口，两种实现：
    - `RealPointCloudSource`：通过 ROS2/MQTT 接收真实设备上报的点云数据（PointCloud2 或自定义格式）。
    - `SimulatedPointCloudSource`：内置点云生成器，按沙盘预建模型 + 模拟扫描轨迹，逐步吐出点云片段（模拟SLAM建图）。
  - 通过配置文件切换两种实现，业务逻辑无感知。
  - 后端做点云聚合、坐标变换、降采样后，通过 WebSocket 推送增量点云到前端。
- **前端**：
  - Three.js（@react-three/fiber）用 `Points` 渲染点云，随扫描进度逐渐"长出"3D地图。
  - 点云与透明空间方块模型叠加：点云为"扫描实时建图"，方块为"预建占位参考"。
  - 支持点云密度/透明度调节。

### 6.5 数据库设计要点

- **PostgreSQL 16**：主库，存储设备注册表、任务、剧本、用户、地图配置等关系数据。
- **TimescaleDB**（PG扩展）：存储遥测事件流（设备位置、电量、状态、告警事件），自动分区压缩，支持时序聚合查询（用于报表和趋势图）。
- **Redis**：
  - 设备实时状态热数据（最新位姿、电量、状态）缓存，避免每秒查库。
  - WebSocket 会话管理、急停命令广播 pub/sub。
  - 调度锁与分布式协调（若未来分布式部署）。
- **Alembic**：保留用于数据库迁移管理。

---

## 七、架构决策记录（ADR）

> 记录关键决策的背景、选项与理由，便于团队对齐与未来回溯。

### ADR-001：采用 Adapter 抽象层 + 内置仿真器

- **状态**：已确认（2026-07-14）
- **背景**：前端开发不能依赖真实机器人联调，需要独立开发节奏。
- **选项**：
  - A. 等真实机器人就绪再开发 → 严重阻塞进度。
  - B. 写死 mock 数据 → 无法验证调度逻辑、协议层。
  - C. Adapter 抽象 + 内置仿真器 → 仿真器按真实协议规范推送数据，真实设备接入只需新增 adapter。
- **决策**：选 C。
- **影响**：后端需实现 `DeviceAdapter` 接口 + `SimulatedDeviceAdapter` 默认实现。

### ADR-002：协议分层（REST + WebSocket + MQTT）

- **状态**：已确认
- **背景**：单一协议无法兼顾可靠控制与实时推送。
- **决策**：REST 用于可靠控制命令，WebSocket 用于前后端实时推送，MQTT 仅用于真实设备遥测接入。
- **影响**：后端需同时维护 REST API、WebSocket hub、MQTT 订阅器（可选）。

### ADR-003：前端采用 pnpm monorepo

- **状态**：已确认
- **背景**：PM端和O&M端共用设备状态、告警、API客户端、UI组件。
- **决策**：`apps/pm` + `apps/om` + `packages/ui` + `packages/three-scene` + `packages/api-client`。
- **影响**：需配置 pnpm workspace + 共享构建配置。

### ADR-004：3D场景使用 @react-three/fiber

- **状态**：已确认
- **背景**：原生 Three.js 与 React 状态同步复杂。
- **决策**：使用 @react-three/fiber + @react-three/drei 声明式集成。
- **影响**：增加 React 依赖，但大幅降低状态管理复杂度。

### ADR-005：数据库选型 PostgreSQL + TimescaleDB + Redis

- **状态**：已确认
- **背景**：用户要求使用实际落地业务级数据库，不只考虑演示。
- **选项**：
  - A. SQLite → 演示够用但不实用，无法支撑并发与时序。
  - B. PostgreSQL + Redis → 关系型够，但时序数据查询性能差。
  - C. PostgreSQL + TimescaleDB + Redis → 一套 PG 搞定关系+时序，运维简单。
- **决策**：选 C。
- **影响**：需部署 PG + TimescaleDB 扩展 + Redis，docker-compose 编排。

### ADR-006：3D点云渲染采用混合模式（路径C）

- **状态**：已确认
- **背景**：用户希望3D场景由机器人扫描点云渲染，但当前无真实点云源。
- **决策**：后端 `PointCloudSource` 抽象，演示用仿真生成器，未来切真实点云。
- **影响**：需实现点云生成器（基于沙盘预建模型 + 扫描轨迹模拟）+ Three.js 点云渲染管线。

### ADR-007：权限分级——PM端仅应急急停

- **状态**：已确认
- **背景**：用户要求仅 O&M 可操控机器人状态。
- **决策**：PM端保留应急急停按钮（安全兜底），但恢复必须 O&M 端确认；其余机器人控制全在 O&M 端。
- **影响**：前端需实现权限守卫，后端需校验操作权限。

### ADR-008：调度策略一期市场机制

- **状态**：已确认
- **背景**：市场机制实现成本可控，RL 训练周期长。
- **决策**：一期实现 `MarketSchedulerStrategy`，预留 `SchedulerStrategy` 接口。
- **影响**：二期可实现 `RLSchedulerStrategy` 替换。

---

## 八、Non-Goals（非目标）

> 明确"不做什么"，避免范围蔓延。

本系统**不做**以下内容：

1. **机器人硬件本体**：不设计、不制造机器人硬件，只提供接入API。
2. **定位算法**：不实现 SLAM / 二维码识别 / 视觉定位算法本身，只接收设备上报的坐标并做越界校验。
3. **真实CV识别算法**：一期不实现摄像头AI识别算法（挖机数、人员数、扬尘），用模拟数据，预留接口。
4. **生产级权限体系**：不做细粒度RBAC、SSO、审计日志体系，只做 PM/O&M 两级简单权限。
5. **高并发互联网架构**：目标 20-100 设备并发，不做百万级并发的水平扩展设计。
6. **多租户**：单租户单项目部署，不做 SaaS 多租户。
7. **移动端App**：只做 Web 大屏（PC端），不做移动端App。
8. **教学/实验部分软件**：机械臂砌砖的教学/实验子模块软件不在本系统范围。
9. **真实砂浆/灰缝工艺模拟**：不模拟真实砌筑工艺，调度系统只派发"砌筑"任务，工艺细节由机器人执行端实现。
10. **PDF报表**：一期只做 Excel 导出，PDF 报表二期。

---

## 九、本系统开发流程（待执行）

> 与用户约定的开发阶段顺序。

1. ✅ **项目认知完善性评估**（已完成，见第六~八章）。
2. ✅ **关键决策确认**（已完成，见 ADR）。
3. ⏳ **技术框架确认**：基于决策推荐合适的 Skill 与技术栈清单，向用户确认。
4. ⏳ **软件设计 + 用户场景设计**：产出系统架构图、模块划分、用户场景剧本，向用户确认方案可行性。
5. ⏳ **界面交互设计**：PM端 + O&M端交互流程设计，向用户确认。
6. ⏳ **实现**：按确认后的设计进行编码。
