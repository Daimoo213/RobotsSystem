# 本机项目阶段报告

更新时间：2026-07-21

## 当前运行状态

按本次要求，当前部署已经停止：

| 项目 | 当前状态 |
| --- | --- |
| 后端 8000 | 已停止，无监听 |
| PM 5173 | 已停止，无监听 |
| O&M 5174 | 已停止，无监听 |
| PostgreSQL 5432 | 容器已停止，无监听 |
| Redis 6379 | 容器已停止，无监听 |
| 管理器状态文件 | 无受管服务记录 |

停止过程保留了开发数据库和 Redis 的 Docker 卷，没有删除业务数据。旧开发卷与新增完整部署卷相互隔离，不会自动复制或覆盖。

## 当前代码阶段

源码已具备真实设备网关、数据库任务调度、设备命令与 ACK、地图/点云接入、PM/O&M 前端、Alembic 迁移和真实数据库报表。平台不会自动创建演示项目、机器人、任务或遥测。

本次已完成部署工具链重构：

- 本机开发 Compose 只运行 PostgreSQL/Redis，并仅绑定 `127.0.0.1`。
- 完整部署包含 PostgreSQL、Redis、后端、PM 和 O&M，前端由 Nginx 提供。
- 完整部署自动生成本机随机密钥并拒绝占位符。
- 后端就绪检查真实验证 PostgreSQL 和 Redis，前端健康检查验证 API 代理。
- 部署启动等待依赖健康，迁移失败不会宣告成功。
- 停止保留数据卷，支持主机备份和显式确认恢复。
- 开发管理器增加进程指纹核验、依赖顺序、优雅停止和非零失败退出码。

## 尚未完成的现场条件

代码完成不等于现场验收完成。以下内容必须由项目实施方提供真实信息或设备后才能验收：

- 真实项目编码、位置、地图坐标系、区域和施工点位。
- PM/O&M 实际人员账号和密码管理制度。
- 机器人或外部 Gazebo 桥接程序及每台设备能力清单。
- 真实地图、点云、相机/视觉检测源。
- 局域网地址、防火墙、TLS/反向代理和备份存储策略。
- 急停、硬件安全回路、定位和任务全生命周期联调。

## 当前外部阻塞

本机 Docker Engine 正常，但当前无法连接 Docker Hub，且尚未缓存 `python:3.12-slim`、`node:22-alpine`、`nginx:1.27-alpine`。因此完整部署镜像尚未在本机完成实际构建。`deploy.py doctor` 会在启动前明确返回失败，不会继续到半完成部署。配置 Docker Desktop HTTPS 代理或现场允许的镜像仓库后，再执行 `doctor` 和 `up`。

## 下一次启动

本机开发：

```powershell
py -3.10 manager\manager.py init-env
py -3.10 manager\manager.py doctor
py -3.10 manager\manager.py start all
```

完整部署：

```powershell
py -3.10 manager\deploy.py init
py -3.10 manager\deploy.py doctor
py -3.10 manager\deploy.py up
```

完整说明见 [部署与运维手册](部署与运维手册.md)。
