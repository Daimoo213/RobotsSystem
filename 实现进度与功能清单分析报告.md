# 机器人集群智能调度系统 — 实现进度与功能清单分析报告

> 分析对象：`D:\MengWork\Web\RobotsSystem`（科研沙盘模拟项目 · 集群调度系统）
> 对照基准：`方案一_集群调度系统开发方案.docx`（规划方案）+ `CODEBUDDY.md`（项目认知）
> 分析方法：对 `backend/`（Python 后端）、`frontend/`（pnpm monorepo 双端）逐文件静态走查，并与规划方案 8 大模块逐项比对
> 分析时间：2026-07-16

---

## 一、执行摘要（总体结论）

本项目已构建出一个**可运行的演示/科研展示型原型**，核心调度链路（设备仿真 → DAG 依赖解锁 → 市场机制派单 → 实时进度回写 → WebSocket/大屏推送）已端到端打通，且经后端日志验证各 REST/WS 端点均正常响应。

| 维度 | 完成度估算 | 关键结论 |
|------|-----------|---------|
| **后端** | ~70% | 基础设施与智能调度模块最完整；真实设备接入、安全/协同执行逻辑、告警生成引擎、维保/续航预测、工程化（迁移/测试）缺位 |
| **前端 PM 端** | ~80% | 三大栏 + 甘特图 + KPI + 待办/趋势 已实现且数据驱动；摄像头为占位、部分按钮未接线、待办标签体系不完整 |
| **前端 O&M 端** | ~65% | 3D 点云 + 设备标记 + 状态环图 + 设备条 + 告警/拓扑 已落地；透明方块模型缺失、暂停/释放/重置按钮未接线、三视图仅切标签、历史告警/轨迹回放为空 |
| **机器人（设备层）** | ~5% | 仅 `SimulatedAdapter` 仿真器，无任何真实 ROS2/MQTT 硬件对接；点云/摄像头为占位 |

**最突出的三类缺漏**：① 真实机器人接入层（方案核心交付物之一）完全空白；② 安全/协同的"执行闭环"（碰撞联锁、人机避让、电子围栏越界拦截）只有配置无逻辑；③ 告警引擎"能查不能生"，维保/续航预测为空壳。

---

## 二、按模块的实现进度评估

### 基础设施（跨模块）
| 项 | 状态 | 证据 |
|----|------|------|
| API 框架（FastAPI） | ✅ 完成 | `backend/app/main.py`：lifespan + CORS + 12 路由挂载 + `/health` |
| 数据库模型（SQLAlchemy 2.0 async） | ✅ 完成 | `models/models.py`：User/Device/Task/Script/Alert/MapRegion/MapPoint/Camera/CameraDetection/DeviceEvent 10 张表 |
| 建表 + 种子数据 | ✅ 完成 | `models/base.py`：`create_tables()` + `seed_demo_data()`（20 设备/4 区域/6 点位/29 任务/3 摄像头） |
| 实时通信（Redis pub/sub + WebSocket） | ✅ 完成 | `core/redis.py`（7 频道）、`ws/router.py` |
| 鉴权（JWT + 角色权限矩阵） | ✅ 完成 | `core/security.py`：PM/OM 两级 24 项权限 |
| 配置/日志/容错 | ✅ 完成 | `core/config.py`、`core/logging.py`（structlog） |
| Docker 部署 | ✅ 完成 | `Dockerfile` + `docker-compose.yml`（postgres:16 + redis:7 + backend） |
| **Alembic 迁移** | ❌ 缺失 | 无 `alembic/` 目录；生产仅靠 `Base.metadata.create_all` |
| **自动化测试** | ❌ 缺失 | 全仓无 `test_*.py` |
| **空目录** | ⚠️ | `backend/scripts/` 无任何文件 |

---

### 模块 1 · 设备接入与集群管理 — 部分完成（~70%）
- ✅ 已实现：`adapters/base.py` 定义 `DeviceAdapter` 抽象基类（6 个抽象方法）；`models/models.py:Device` 含"一机一档"字段（型号/能力/标段权限/状态/电量/坐标/健康）；心跳遥测（`adapters/simulated.py:_heartbeat` 写 Redis、每 5 tick 持久化 `device_events`）；按 status/type 过滤。
- ⚠️ 部分：分组仅为静态 `section_id`/`section_tags` 字段，无分组管理 API；设备仅由种子数据预置。
- ❌ 未实现：**无设备运行时注册端点**（缺 `POST /api/devices`）；**无真实设备接入**——仅有 `SimulatedAdapter`，`Ros2Adapter` 从未实现（`adapters/__init__.py` 仅导出 Simulated）；`config.device_adapter="ros2"` 选项选了会直接失败。

### 模块 2 · 工地地图与环境感知 — 部分完成（~60%）
- ✅ 已实现：`MapRegion`/`MapPoint` 模型 + `api/map.py`（区域含 work/stack/parking/restricted，点位含 pit/material/exit/charge + process_type + device_types）；`/map/scene-config` 返回建筑/基坑/堆场/围挡/缓冲区摆放；`/dashboard/environment` 提取扬尘/温度。
- ⚠️ 部分：地图以"多边形区域 + 离散点位"表达。
- ❌ 未实现：**无"二维网格 + 高度层"地图数据结构**；**动态障碍无模型/端点/更新逻辑**（仅有静态 barrier/buffer）；区域与点位均为种子数据，无 CRUD。

### 模块 3 · 集群智能调度（核心）— 基本完成（~90%）
- ✅ 已实现（真实算法）：`scheduler/dag.py:TaskDAG` 完整依赖图（解锁/就绪/环检测）；`models/base.py:PROCESSES` 29 项工序；`scheduler/engine.py:SchedulerEngine` 后台循环；`scheduler/market.py:MarketSchedulerStrategy.allocate()` **成本函数** `cost = distance_cost*0.5 + battery_cost*0.3 + load_cost` 真实派单；`Script` 调度剧本 + 激活重载 DAG；优先级 + 人工干预（创建/改派/暂停/恢复/插单）；`tasks.py:PROCESS_OPTIONS` 29 工序下拉。
- ⚠️ 部分：`Script` 仅含 tempo 配置，非可复用"任务模板"。
- ❌ 未实现：**分布式调度未落地**（仅有集中式 `SchedulerEngine`，`SchedulerStrategy` 协议仅市场一种实现；方案要求的"分布式协商避碰"缺失）；强化学习派单未实现（仅预留接口）。

### 模块 4 · 集群协同作业控制 — 部分完成（~40%）
- ✅ 已实现：批量操控 `adapters/simulated.py:send_command(device_id=None)` 遍历全设备 → pause/resume/estop/reset/release；单设备/全量命令端点。
- ❌ 未实现：**编队同步**——全代码库无 formation 逻辑；**碰撞联锁**——`config.device_safety_distance=2.0` 已定义但**从未被读取/使用**（grep 确认），无设备间距离检测与减速/停车逻辑。

### 模块 5 · 能耗与维保管控 — 部分完成（~45%）
- ✅ 已实现（仿真级）：`adapters/simulated.py` 电池随机游走耗电/充电；电量<15 自动回充；`runtime.force_charge` 运维指令。
- ⚠️ 部分：运维指令骨架 `ops.py`（8 端点）。
- ❌ 未实现：**续航预测算法缺失**；**维保提醒缺失**；`backup`/`firmware_upgrade`/`mode_switch` 运维命令为**空壳**（直接返回硬编码字符串，不做任何处理，见 `runtime.py:193-205`）。

### 模块 6 · 监控与告警中心 — 部分完成（~65%）
- ✅ 已实现：数字孪生数据源 `api/dashboard.py`（8 项 KPI、计划/实际/预测三线趋势、资源负载、健康分、摄像头列表，计算逻辑详尽）；单设备下钻 `GET /devices/{id}`；`Alert` 模型 + 列表/确认接口。
- ❌ **关键缺口 — 告警引擎未实现**：grep `Alert(` 仅出现在模型定义，**无任何代码生成告警记录**。仿真器仅把健康置为 `"warn"` 但不写 `alerts` 表；急停只发 WS 事件不发告警。前端 `AlertList` 实际恒为空。即"能看不能生"。

### 模块 7 · 施工数据与报表统计 — 部分完成（~75%）
- ✅ 已实现：`DeviceEvent` 时序模型 + `base.py` 尝试建 TimescaleDB hypertable（失败则跳过）；仿真器持续写入遥测；多维聚合（`dashboard.py` 按阶段/类型/日，`reports.py` 按阶段/类型）；**Excel 导出** `reports.py:export` 用 openpyxl 生成 5 sheet。
- ❌ 未实现：**PDF 导出缺失**（仅 Excel，`report_type` 参数仅作文件名）；无按时间范围查询 `device_events` 的 REST 端点（仅内部写入）。

### 模块 8 · 施工现场安全管控 — 部分完成（~55%）
- ✅ 已实现：**全局急停完成**（`estop.py` + `runtime.trigger_estop/recover_estop` 置全局标志、停调度、广播 WS、记事件）；电子围栏数据模型（`MapRegion.region_type="restricted"`）；`safety_inspect` 返回 `fences_ok=True`。
- ❌ 未实现：**电子围栏无执行**——适配器移动逻辑 `_move_towards_target` 不检查是否进入 restricted 区域，无越界告警/拦截；**人机避让未实现**——摄像头 `person_count` 未用于避让决策。

---

## 三、规划方案 vs 当前实现对比 —— 已规划但尚未实现的功能缺漏

以下为方案明确要求、但代码中**完全缺位或仅占位**的功能（按优先级排序）：

| # | 缺漏功能 | 方案出处 | 现状 |
|---|---------|---------|------|
| 1 | **真实机器人接入层（ROS2/MQTT 驱动与协议适配）** | 核心交付物、模块5.1、技术路线 | 仅 `SimulatedAdapter`，`Ros2Adapter` 不存在；`mqtt_enabled=false` 且 aiomqtt 未接线 |
| 2 | **碰撞联锁（设备间位姿交换 + 安全距离减速/停车）** | 模块5.4、配置 `device_safety_distance` | 配置定义但**零引用**，无任何距离计算 |
| 3 | **电子围栏越界拦截 / 人机避让** | 模块5.8 | 围栏仅数据模型；移动逻辑不校验禁行区；人员检测不触发避让 |
| 4 | **告警生成引擎（离线/超时/低电量/路径冲突/越界分级推送）** | 模块5.6 | 仅有查询接口，无任何产生逻辑，告警列表恒空 |
| 5 | **编队同步作业** | 模块5.4 | 全代码库无 formation 逻辑 |
| 6 | **维保提醒（按里程/时长触发工单）** | 模块5.5 | 未实现 |
| 7 | **续航/电量预测** | 模块5.5 | 未实现 |
| 8 | **分布式调度/协商（局部资源争用与避碰）** | 模块7.1、5.3 | 仅集中式；分布式仅协议预留 |
| 9 | **二阶网格+高度层地图 + 动态障碍实时更新** | 模块5.2 | 仅有多边形区域/静态点位 |
| 10 | **任务模板（可复用工作流一键派单）** | 模块5.3、7.3 | `Script` 仅 tempo 配置，不可复用 |
| 11 | **三维"透明空间方块"沙盘模型（楼栋/基坑/堆场/围挡/缓冲区）** | O&M 端 3D 视角 | 仅有原始点云 + 网格，无结构化半透明模型 |
| 12 | **PDF 报表导出** | 模块5.7 | 仅 Excel |
| 13 | **摄像头真实视频流（CCTV + AI 识别浮窗数据源）** | 双端摄像头 | PM 端为占位框；后端无 RTSP/HLS 接入，AI 识别为模拟 |
| 14 | **真实点云采集（深度相机/激光雷达建图）** | 模块5.2、点云服务 | `pointcloud/manager.py:real` 分支为 `Placeholder for future implementation` |
| 15 | **Alembic 数据库迁移** | 工程化 | 无，生产靠 `create_all` |
| 16 | **自动化测试（pytest）** | 工程化 | 全仓零测试 |
| 17 | **O&M 端 暂停/释放/重置 命令接线** | O&M 场景工具栏 | `SCENE_COMMANDS` 含 5 项，仅 estop/resume 有 onClick |
| 18 | **调度/能耗/安全 三视图实际内容切换** | O&M 顶部标签 | 仅切 `currentView` 状态，画面无差异 |
| 19 | **设备历史告警 / 轨迹回放** | O&M 设备下钻 | 历史告警硬编码"暂无"；轨迹回放按钮无逻辑 |
| 20 | **PM 端待办任务多分类标签体系（AI/财务/审批/环保/安全/合约/实名制…）** | PM 右栏 | 仅 紧急/进行中/已暂停 三类 |
| 21 | **PM/O&M 甘特图"导出/图层"按钮、任务详情"改派"动作** | 双端交互 | 按钮/动作未接线 |

---

## 四、三维度功能清单与技术选型

以下按「前端 / 后端 / 机器人」三维度，归纳**当前需补齐（及已选待固化）**的全部功能，逐条给出技术选型与理由。已完成项标注"已实现"，主要聚焦"待实现/待补齐"的选型建议。

### 4.1 后端功能清单与技术选型

| 功能点 | 状态 | 技术选型建议 | 选择理由 |
|--------|------|-------------|---------|
| 设备接入抽象层 DeviceAgent | 已实现（仅仿真） | 抽象基类 `DeviceAdapter` + 新增 `Ros2Adapter` / `MqttAdapter` | 适配器模式已搭好骨架，扩展具体实现即可对接多厂商，不改调度核心 |
| **真实 ROS2 设备对接** | 未实现 | `rclpy` + 自定义 ROS2 消息（`DeviceState`/`DeviceCommand`）；用桥接进程订阅 `/device_*/state`、发布 `/device_*/cmd` | 方案指定 ROS2 为主框架；Python 原生 rclpy 与现有 FastAPI 同语言，集成成本低 |
| **MQTT 遥测桥接** | 未实现 | `aiomqtt`（依赖已装）+ `paho-mqtt` 兜底；设备经 `robots/{id}/telemetry` 上报 | 轻量、适合沙盘大量低功耗节点；与 Redis pub/sub 解耦 |
| 设备运行时注册 / 一机一档 CRUD | 未实现 | FastAPI `POST/PUT/DELETE /api/devices` + 注册表写库 | 方案要求"批量注册"，当前仅种子数据，需开放运行时接入 |
| 心跳 + 遥测采集 | 已实现（仿真） | 设备侧定时上报 → Redis 缓存 + 周期落 `device_events` | 已验证可用，真实设备复用同一契约 |
| 地图：网格+高度层模型 | 未实现 | PostGIS `geometry`/`raster` 或纯 numpy 网格 + z 层；提供 `/map/grid` | 支撑路径规划与禁行区校验；沙盘尺度可用内存网格，无需重 GIS |
| 动态障碍管理 | 未实现 | `MapObstacle` 模型 + 传感器/视觉回调更新 + WS 推送 | 方案要求实时标注临时堆放/人员 |
| **调度引擎（集中式）** | 已实现 | `SchedulerEngine` 后台 loop（asyncio） | 已落地，保留 |
| **市场机制派单** | 已实现 | 成本函数 `distance*0.5 + battery*0.3 + load`（可配置权重） | 已验证，保留并抽象为可切换策略 |
| **分布式协商/避碰** | 未实现 | ROS2 多节点 + 基于 Redis 的协商锁 / 拍卖二阶；或引入 `RAY`/分布式任务队列 | 满足方案"集中+分布混合"；局部资源争用实时性要求高，需去中心化协商 |
| **碰撞联锁** | 未实现 | 调度器 tick 内计算设备两两欧氏距离，对照 `safety_distance` 下发减速/停车指令 | 配置已就绪，只需在 `SchedulerEngine` 增加距离检测闭环 |
| 任务模板 | 未实现 | `TaskTemplate` 模型 + 模板实例化接口 | 固化常见工作流，支持一键批量派单 |
| 人工干预留痕 | 已实现 | 干预记录写入 `DeviceEvent`/`AuditLog` | 已支持，补全审计展示 |
| **续航/电量预测** | 未实现 | 基于能耗模型的线性外推 / 卡尔曼滤波，输出 `eta_minutes` | 沙盘尺度简单模型即可，支撑"低电量不派长途" |
| **维保提醒** | 未实现 | 累计 `device_events` 里程/时长 → 触发 `MaintenanceOrder` | 方案要求按里程/时长保养工单 |
| 错峰充能 | 已实现（仿真） | 空闲/低优先间隙调度充电 | 保留 |
| **告警引擎** | 未实现 | `AlertEngine` 订阅各模块事件，按规则（离线/超时/低电量/路径冲突/越界）生成 `Alert` 落库 + WS 推送 | 当前最大展示短板；规则引擎轻量（阈值+状态机）即可 |
| 电子围栏越界拦截 | 未实现 | 移动指令前校验目标点是否落入 `restricted` 区域，越界则拒发+告警 | 安全底线，逻辑简单收益高 |
| 人机避让 | 未实现 | 摄像头 `person_count`/位姿 → 危险区减速/停机 | 复用安全管控链路 |
| 全局急停 | 已实现 | `estop.py` + 全局标志 | 保留 |
| 数字孪生大屏数据源 | 已实现 | `dashboard.py` 聚合 + WS | 保留 |
| 时序库与事件溯源 | 已实现 | TimescaleDB hypertable（兜底普通表） | 保留 |
| **报表导出 PDF** | 未实现 | `reportlab`（矢量报表）或 `WeasyPrint`（HTML→PDF） | 与现有 openpyxl Excel 并行；科研汇报需 PDF |
| **Alembic 迁移** | 未实现 | 引入 `alembic` + `alembic.ini`，`models` 改为迁移生成 | 生产必备，避免 `create_all` 丢数据 |
| **自动化测试** | 未实现 | `pytest` + `httpx.AsyncClient` + mock `SimulatedAdapter`；CI 跑 `pytest` | 当前零测试，调度算法与告警引擎需回归保障 |
| 点云真实采集 | 未实现 | 后端接入深度相机/激光雷达点流（ROS2 `/scan` 或文件流）→ 体素化 | 替换 `pointcloud/manager.py` 的 placeholder |

### 4.2 前端功能清单与技术选型

**技术栈（已实现，建议固化）**：React 18 + TypeScript + Vite + Tailwind CSS；状态 `zustand`；实时 `WebSocket` 自研客户端（自动重连/频道订阅）；共享包 `api-client` / `shared-types` / `ui` / `utils` / `three-scene`。

#### PM 端

| 功能点 | 状态 | 技术选型建议 | 选择理由 |
|--------|------|-------------|---------|
| 顶部状态栏 + 8 项 KPI | 已实现 | 轮询 `/dashboard/kpi`（5s） | 数据驱动，保留 |
| 左栏：机器人分布/状态/资源负载/阶段总览 | 已实现 | 手绘 SVG 环形图（DonutChart）+ 进度条 | 轻量、零额外依赖，保留 |
| **中栏甘特图** | 已实现（强） | 自研 SVG 甘特（日/周/月/季度、依赖线、里程碑、今日线） | 已非常完整；如需维护可迁 ECharts-Gantt，但现状够用 |
| 右栏待办任务 + 新增 | 已实现 | `createTask` 弹窗 + store 同步 | 保留 |
| **待办多分类标签体系** | 未实现 | 在 `Task` 增加 `tags: string[]`，UI 渲染 紧急/今日/AI/财务/审批/巡检/环保/安全/合约/实名制 等色标 | 方案要求丰富分类，提升"讲得清" |
| 摄像头视角 + AI 浮窗 | 部分 | 接入 HLS/WebRTC 播放 RTSP（`hls.js`/`video.js`）；AI 浮窗绑定 `latest_detection` | 当前为占位框，需真实视频流 |
| 完成趋势三线图 | 已实现 | 手绘 SVG polyline | 保留 |
| 实时事件流 | 已实现 | `EventFeed` + WS `events` 频道 | 保留 |
| **甘特图导出/图层按钮、任务详情改派** | 未实现 | 导出调用 `/reports/export`；改派调用 `reassign` 接口 | 按钮已存在，补 onClick |

#### O&M 端

| 功能点 | 状态 | 技术选型建议 | 选择理由 |
|--------|------|-------------|---------|
| 顶部状态 + 三视图标签 | 已实现（标签） | 三视图切换需驱动不同数据/图层 | 见下"三视图内容" |
| 左栏状态环图/运维台/日志 | 已实现 | DonutChart + `runOpsCommand` + EventFeed | 保留；运维指令后端补齐后自然生效 |
| **中栏 3D 场景** | 部分 | `@react-three/fiber` + `@react-three/drei`；**新增半透明方块模型层**（楼栋/基坑/堆场/围挡/缓冲区用 `mesh`+`transparent` 材质），叠加现有点云 + 设备标记 | 方案核心是"透明空间方块视图"，当前仅有裸点云，需补结构化模型 |
| **场景工具栏 暂停/释放/重置** | 未实现 | 复用 `sendCommand`/`runtime` 指令接 API | `SCENE_COMMANDS` 已定义，仅补 onClick |
| **三视图实际切换** | 未实现 | 按 `currentView` 切换场景图层/数据（调度=全量；能耗=按电量着色；安全=叠加围栏+告警热区） | 方案要求三视图差异化呈现 |
| 右栏拓扑/类型分布/告警/概览 | 已实现（基础） | SVG 星型拓扑（建议升级为按协同关系分层布局）；AlertList 绑 WS `alerts` | 拓扑当前为简单星形，告警待后端引擎填充 |
| 底部 18 设备卡片 | 已实现 | `DeviceCard` 含 5 项体检指标 | 保留（建议显示全量设备而非仅 18） |
| **设备历史告警/轨迹回放** | 未实现 | 新增 `/api/devices/{id}/alerts`、`/trajectory`；回放用 Three.js 路径动画或 SVG | 详情面板已有占位区，补数据与播放逻辑 |
| 急停覆盖层 | 已实现 | `EstopOverlay` | 保留 |

### 4.3 机器人（设备层）功能清单与技术选型

> 本维度我方**仅提供接入标准与接口**，不实现硬件本身，但需补齐"让硬件能连进来"的适配层与协议规范。当前完成度极低（~5%）。

| 功能点 | 状态 | 技术选型建议 | 选择理由 |
|--------|------|-------------|---------|
| **设备接入标准/统一协议** | 未实现 | 定义 JSON Schema：`DeviceState{id,type,status,battery,position,health}` / `DeviceCommand{cmd,payload}`；MQTT topic `robots/{id}/{telemetry\|cmd}` 或 ROS2 话题 | 方案要求"统一抽象为 DeviceAgent"，需先定契约，多厂商才能即插即用 |
| **Ros2Adapter 设备驱动** | 未实现 | `rclpy` 节点：订阅 `device_state`、发布 `device_cmd`；适配 AGV/挖掘机/吊车/砌筑/巡检五类 | 方案指定 ROS2 为主；与各设备厂商 SDK 对接的落点 |
| **MQTT 适配器（轻量节点）** | 未实现 | `aiomqtt` 订阅遥测、发布指令 | 沙盘低功耗/微控制器节点常用 MQTT |
| **心跳与遥测上报规范** | 未实现 | 设备侧 1–2s 心跳 + 事件驱动遥测；QoS1 | 对接后端"心跳+遥测"采集契约 |
| **SLAM / 二维码定位** | 未实现 | 设备端定位 → `position{x,y,z}` 上报；或后端融合 | 方案指定定位方式，是地图/路径前提 |
| **指令执行与状态回执** | 未实现 | 设备收到 `DeviceCommand` 后回报 `ack` + 执行结果 | 保障"派单→执行→反馈"闭环 |
| **急停/安全链路** | 未实现 | 设备固件监听全局急停主题，硬线+软停双保险 | 安全兜底，方案强制要求 |
| **点云/视觉真实源** | 未实现 | 深度相机/激光雷达 → ROS2 `/scan`/`/points` → 后端体素化 | 替换占位点云，支撑 3D 大屏 |
| **摄像头视频流** | 未实现 | RTSP/ONVIF → 转 HLS（`ffmpeg`/媒体网关）→ 前端 `hls.js` | 支撑 PM 端 CCTV + AI 识别 |

---

## 五、关键风险与建议优先级

**P0（演示/验收阻断，必须补齐）**
1. 真实机器人接入层（ROS2/MQTT Ros2Adapter）——否则"大脑"永远只连仿真，方案核心交付物缺失。
2. 告警生成引擎——当前大屏"告警"恒为空，是客户最在意的"看得见异常"能力。
3. 碰撞联锁 + 电子围栏越界拦截——安全底线，沙盘演示若发生"打架"将直接失败。

**P1（展示完整度，强烈建议）**
4. O&M 3D 透明方块模型 + 三视图真实切换——方案视觉核心，当前仅裸点云。
5. 续航预测 + 维保提醒——能耗维保模块从"仿真随机游走"升级为"可解释管控"。
6. 摄像头真实视频流 + AI 识别数据源。
7. PDF 报表导出。

**P2（工程化与长期可维护）**
8. Alembic 迁移、自动化测试（pytest）。
9. 分布式调度/协商、任务模板、动态障碍、历史告警/轨迹回放、待办多标签等体验增强。

**一句话总结**：后端"大脑逻辑"已成形（调度算法真实可用），前端双端骨架与数据联动已立起，但**从"能跑的演示原型"到"方案承诺的科研样机"之间，最关键的真实设备接入、安全执行闭环、告警引擎三块仍是空白**，应优先投入。

---

*注：本报告基于 `backend/`、`frontend/` 源码静态走查与方案文档比对，未执行前端构建与后端集成测试；"实现状态"判定以代码中存在真实逻辑（而非占位/空壳）为准。*
